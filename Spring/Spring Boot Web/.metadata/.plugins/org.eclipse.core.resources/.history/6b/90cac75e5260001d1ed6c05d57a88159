<%@ page language="java" contentType="text/html; charset=ISO-8859-1"
	pageEncoding="ISO-8859-1"%>
<%@ page
	import="java.util.List, com.epam.entity.Quiz, com.epam.entity.Question, com.epam.entity.Option"%>
<!DOCTYPE html>
<html>
<head>
<meta charset="ISO-8859-1">
<title>Insert title here</title>
</head>
<body>
	<div>
		<span><a href="/">Home</a></span>
		<span><a href="/user/loginpage">Back</a></span>
	</div>
	<%
	Quiz quiz = (Quiz) request.getAttribute("quiz");
	if (quiz == null) {
	%>
	<div>No quiz available !</div>
	<%
	} else {
	List<Question> questionList = quiz.getQuestionList();
	%>
	<h3><%=quiz.getQuizTitle()%></h3>
	<%
	if (questionList.isEmpty()) {
	%>
	Quiz is empty.

	<%
	} else {
	for (int j=0; j<quiz.getQuestionList().size(); j++) {
	%>

	<div>
		<form action="/admin/deletequestion" method="post">
			<input type="hidden" name="quizid" value="<%=quiz.getQuestionList().get(j).getId()%>" /> <input
				type="hidden" name="questionid" value="<%=quiz.getQuestionList().get(j).getId()%>" /> <span><%=quiz.getQuestionList().get(j).getId()%>.
			</span> <span><%=quiz.getQuestionList().get(j).getQuestionStatement()%></span>
			<ol>
				<%
				for (int i=0; i<question.getOptionList().size(); i++) {
				%>
				<li>
					<label for="o<%= i %>"><%= question.getOptionList().get(i) %></label>
					<input type="radio" id="<%= i %>" name="o<%= j %>" />
				</li>
				<%
				}
				%>
			</ol>
			<input type="submit" value="delete" />
		</form>
	</div>

	<%
	}
	}
	}
	%>

</body>
</html>